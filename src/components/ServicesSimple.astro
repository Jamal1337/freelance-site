---
const services = [
  {
    title: "Custom Software",
    description: "Internal tools, dashboards, CRMs, and automation systems built for your specific workflows.",
    tags: ["React", "Node.js", "Python", "PostgreSQL"]
  },
  {
    title: "Shopify Development",
    description: "Custom apps, theme development, and headless storefronts for e-commerce brands.",
    tags: ["Shopify Apps", "Liquid", "Hydrogen", "Headless"]
  },
  {
    title: "Landing Pages",
    description: "High-converting pages optimized for paid traffic campaigns. Fast, mobile-first, conversion-focused.",
    tags: ["React", "Vite", "A/B Testing", "Lead Gen"]
  },
  {
    title: "Marketing Tools",
    description: "Attribution systems, analytics dashboards, and reporting tools that show where revenue comes from.",
    tags: ["Meta API", "GA4", "Custom Attribution", "Dashboards"]
  },
  {
    title: "Websites",
    description: "Full websites built with modern tech stacks. From portfolio sites to complex web applications.",
    tags: ["Next.js", "Astro", "TypeScript", "Tailwind"]
  },
  {
    title: "Performance Marketing",
    description: "Meta ads automation, campaign management tools, comment moderation, and optimization systems.",
    tags: ["Meta Ads", "Automation", "Bid Rules", "Reporting"]
  }
];
---

<section id="services" class="services">
  <div class="services-container">
    <!-- Header -->
    <header class="services-header">
      <div class="header-left">
        <div class="header-label">
          <span class="label-number">02</span>
          <span class="label-text">Services</span>
        </div>
        <h2 class="services-title">
          What I <span class="title-accent">do</span>
        </h2>
      </div>
      <div class="header-right">
        <p class="services-subtitle">
          Development and marketing for e-commerce and lead generation. From landing pages to complex automation.
        </p>
      </div>
    </header>

    <!-- Services Grid -->
    <div class="services-grid">
      {services.map((service) => (
        <div class="service-card">
          <h3 class="service-title">{service.title}</h3>
          <p class="service-description">{service.description}</p>
          <div class="service-tags">
            {service.tags.map((tag) => (
              <span class="service-tag">{tag}</span>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .services {
    background: #0c0c0c;
    padding: clamp(5rem, 12vw, 10rem) 0;
    position: relative;
  }

  .services-container {
    max-width: 1800px;
    margin: 0 auto;
    padding: 0 clamp(1.5rem, 5vw, 4rem);
  }

  /* Header */
  .services-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: end;
    margin-bottom: clamp(4rem, 8vw, 6rem);
    padding-bottom: clamp(2rem, 4vw, 3rem);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .header-label {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .label-number {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: #a7a9ac;
    letter-spacing: 0.05em;
  }

  .label-text {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: #ffffff;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .services-title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 6vw, 5rem);
    font-weight: 700;
    color: #ffffff;
    line-height: 1.05;
    letter-spacing: -0.03em;
  }

  .title-accent {
    color: var(--color-accent);
  }

  .services-subtitle {
    font-family: var(--font-body);
    font-size: clamp(1rem, 1.25vw, 1.125rem);
    color: #a7a9ac;
    max-width: 400px;
    margin-left: auto;
    margin-bottom: 0;
  }

  /* Services Grid */
  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(1rem, 2vw, 1.5rem);
  }

  .service-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    padding: clamp(1.5rem, 3vw, 2rem);
    transition: all 0.4s var(--ease-out-quart);
  }

  .service-card:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: var(--color-accent);
  }

  .service-title {
    font-family: var(--font-display);
    font-size: clamp(1.125rem, 1.5vw, 1.375rem);
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 0.75rem;
    letter-spacing: -0.02em;
  }

  .service-description {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.6;
    color: #a7a9ac;
    margin-bottom: 1.25rem;
  }

  .service-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
  }

  .service-tag {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    letter-spacing: 0.03em;
    color: rgba(255, 255, 255, 0.4);
    padding: 0.25rem 0.5rem;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 3px;
    transition: all 0.3s var(--ease-out-quart);
  }

  .service-card:hover .service-tag {
    background: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.7);
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .services-header {
      grid-template-columns: 1fr;
    }

    .services-subtitle {
      margin-left: 0;
      max-width: 500px;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

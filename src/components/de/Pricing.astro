---
---

<section id="pricing" class="pricing">
  <div class="pricing-container">
    <!-- Header -->
    <header class="pricing-header">
      <div class="header-left">
        <div class="header-label">
          <span class="label-number">06</span>
          <span class="label-text">Investment</span>
        </div>
        <h2 class="pricing-title">
          Flexible Zusammenarbeit
          <span class="title-accent">Modelle</span>
        </h2>
      </div>
      <div class="header-right">
        <p class="pricing-subtitle">
          Jedes Projekt ist anders. Lass uns das passende Modell für deine Anforderungen und dein Budget finden.
        </p>
      </div>
    </header>

    <!-- Pricing Cards -->
    <div class="pricing-grid">
      <!-- Project-Based -->
      <div class="pricing-card">
        <div class="card-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
          </svg>
        </div>
        <span class="card-label">Projektbasiert</span>
        <h3 class="card-title">Fester Umfang</h3>
        <p class="card-description">
          Ideal für klar definierte Projekte mit konkreten Ergebnissen. Du erhältst nach einem Scoping-Call einen Festpreis.
        </p>
        <ul class="card-includes">
          <li>Individuelle Entwicklung</li>
          <li>Klare Meilensteine</li>
          <li>Definierte Timeline</li>
          <li>Vollständige Dokumentation</li>
        </ul>
      </div>

      <!-- Retainer -->
      <div class="pricing-card card-featured">
        <span class="card-badge">Flexibel</span>
        <div class="card-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <span class="card-label">Monatlicher Retainer</span>
        <h3 class="card-title">Laufende Partnerschaft</h3>
        <p class="card-description">
          Für Marken, die kontinuierliche Entwicklung, Support und Iteration brauchen. Priorisierter Zugang mit planbarem monatlichem Investment.
        </p>
        <ul class="card-includes">
          <li>Priorisierter Support</li>
          <li>Kontinuierliche Entwicklung</li>
          <li>Monatlicher Strategie-Call</li>
          <li>Slack/Async Zugang</li>
        </ul>
      </div>

      <!-- Consultation -->
      <div class="pricing-card">
        <div class="card-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
          </svg>
        </div>
        <span class="card-label">Beratung</span>
        <h3 class="card-title">Experten-Rat</h3>
        <p class="card-description">
          Du brauchst Beratung zu Architektur, Tech-Stack oder Strategie? Buche eine Beratung für umsetzbare Insights.
        </p>
        <ul class="card-includes">
          <li>Technische Review</li>
          <li>Architektur-Planung</li>
          <li>Stack-Empfehlungen</li>
          <li>Schriftliche Zusammenfassung</li>
        </ul>
      </div>
    </div>

    <!-- CTA -->
    <div class="pricing-cta">
      <p class="cta-text">Nicht sicher, was du brauchst? Lass es uns gemeinsam herausfinden.</p>
      <a href="#contact" class="cta-button">
        Kostenloses Gespräch buchen
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
          <path d="M3 8H13M13 8L8 3M13 8L8 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .pricing {
    background: #f7f7f7;
    padding: clamp(5rem, 12vw, 8rem) 0;
    position: relative;
  }

  .pricing-container {
    max-width: 1800px;
    margin: 0 auto;
    padding: 0 clamp(1.5rem, 5vw, 4rem);
  }

  /* Header */
  .pricing-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: end;
    margin-bottom: clamp(3rem, 6vw, 4rem);
    padding-bottom: clamp(2rem, 4vw, 3rem);
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }

  .header-label {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .label-number {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: #a7a9ac;
    letter-spacing: 0.05em;
  }

  .label-text {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: #0c0c0c;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .pricing-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 700;
    color: #0c0c0c;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: 1rem;
  }

  .title-accent {
    color: #0c0c0c;
    position: relative;
    z-index: 1;
    display: inline-block;
  }

  .title-accent::before {
    content: '';
    position: absolute;
    bottom: 0.05em;
    left: -0.1em;
    right: -0.1em;
    height: 0.35em;
    background-color: #00FF9C;
    -webkit-mask: url('/brush-stroke.png') center/100% 100% no-repeat;
    mask: url('/brush-stroke.png') center/100% 100% no-repeat;
    z-index: -1;
  }

  .pricing-subtitle {
    font-family: var(--font-body);
    font-size: clamp(1rem, 1.25vw, 1.125rem);
    color: #a7a9ac;
    max-width: 400px;
    margin-left: auto;
    margin-bottom: 0;
  }

  @media (max-width: 768px) {
    .pricing-header {
      grid-template-columns: 1fr;
    }
    .pricing-subtitle {
      margin-left: 0;
      max-width: 500px;
    }
  }

  /* Pricing Grid */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-bottom: clamp(3rem, 6vw, 4rem);
  }

  .pricing-card {
    background: #ffffff;
    padding: clamp(2rem, 4vw, 2.5rem);
    border-radius: 12px;
    border: 1px solid rgba(0, 0, 0, 0.06);
    position: relative;
    transition: all 0.3s var(--ease-out-quart);
  }

  .pricing-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.08);
  }

  .pricing-card.card-featured {
    border-color: var(--color-accent);
  }

  .card-badge {
    position: absolute;
    top: -12px;
    right: 2rem;
    background: var(--color-accent);
    color: #0c0c0c;
    padding: 0.375rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    border-radius: 100px;
  }

  .card-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f7f7f7;
    border-radius: 12px;
    margin-bottom: 1.5rem;
    color: #0c0c0c;
  }

  .card-label {
    display: block;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #a7a9ac;
    margin-bottom: 0.5rem;
  }

  .card-title {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    font-weight: 700;
    color: #0c0c0c;
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
  }

  .card-description {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.6;
    color: #666;
    margin-bottom: 1.5rem;
  }

  .card-includes {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }

  .card-includes li {
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: #333;
    padding-left: 1.5rem;
    position: relative;
  }

  .card-includes li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.5em;
    width: 6px;
    height: 6px;
    background: var(--color-accent);
    border-radius: 50%;
  }

  /* CTA */
  .pricing-cta {
    text-align: center;
  }

  .cta-text {
    font-family: var(--font-body);
    font-size: 1rem;
    color: #666;
    margin-bottom: 1.5rem;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 1.125rem 2.5rem;
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 100px;
    background: #0c0c0c;
    color: #ffffff;
    transition: all 0.4s var(--ease-out-quart);
  }

  .cta-button:hover {
    background: var(--color-accent);
    color: #0c0c0c;
    transform: translateY(-2px);
  }

  .cta-button svg {
    transition: transform 0.3s var(--ease-out-quart);
  }

  .cta-button:hover svg {
    transform: translateX(4px);
  }

  /* Responsive */
  @media (max-width: 968px) {
    .pricing-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
